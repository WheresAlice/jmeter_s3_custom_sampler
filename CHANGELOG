# Changelog
All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## 0.4
### Added
- Allow username, password and scheme to be passed to proxy

## 0.3
### Added
- Return content as ResponseData when no `local_file_path` is set
- Attempt to deserialise avro when returning ResponseData
### Changed
- Dependencies updated to cope with plus symbols in S3 key names

## 0.2
### Added
- Support for listing objects in an S3 bucket

## 0.1.1
### Fixed
- Actually return metadata when using GET

## 0.1
### Added
- Writes to a file when using GET
### Changed
- Package name
- Imports cleaned up
- Typo corrected